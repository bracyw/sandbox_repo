apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-redis
  namespace: ai-chief-of-staff
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task-redis
  template:
    metadata:
      labels:
        app: task-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: task-redis
  namespace: ai-chief-of-staff
spec:
  selector:
    app: task-redis
  ports:
    - port: 6379
      targetPort: 6379
